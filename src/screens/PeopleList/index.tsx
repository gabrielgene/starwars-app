import React from 'react';
import {SafeAreaView, View, Text} from 'react-native';

export default function PeopleList() {
  return (
    <SafeAreaView>
      <View style={{flex: 1}}>
        <Text style={{color: '#000'}}>People List</Text>
      </View>
    </SafeAreaView>
  );
}
